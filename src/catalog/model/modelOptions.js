import { MODELE_LEGENDE, MODELE_PURE } from "./options/models";

export const MODEL_OPTIONS = {
    label: 'Modèle',
    slug: 'modele',
    uniqueOption: true,
    options: [
        MODELE_PURE,
        MODELE_LEGENDE
    ]
}